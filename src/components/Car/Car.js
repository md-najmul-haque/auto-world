import React from 'react';

const Car = ({ car }) => {
    const { _id, carName, mileage, location, engine, transmission, price } = car
    return (
        <div>

        </div>
    );
};

export default Car;